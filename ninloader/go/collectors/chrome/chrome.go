// Package chrome contains collectors for Chrome-level token and cookie
// extraction via the Chrome DevTools Protocol (CDP).
//
// cdp_cookies.go registers on all platforms via its init().
package chrome
